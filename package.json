{
  "name": "ru-news-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "check": "node --check app.js && node --check middleware.js && node --check api/news.js && node --check api/health.js && node --check api/claude.js && node --check api/login.js && node --check api/logout.js && node --check lib/auth.js && node --check lib/fetch-news.js && node --check lib/sources.js",
    "verify": "node tools/verify-sources.mjs",
    "hash-password": "node tools/hash-password.mjs"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "vercel": "^44.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
